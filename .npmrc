# @gregoiref packages are published to GitHub Packages.
# NODE_AUTH_TOKEN (set to GITHUB_TOKEN in CI) provides authentication.
@gregoiref:registry=https://npm.pkg.github.com

# Suppress the funding message
fund=false
